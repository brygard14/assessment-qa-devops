const {shuffleArray} = require('./utils')

describe('shuffleArray should return an array', () => {
    // CODE HERE
    test('return the itmes in array in any order', () => {
        expect(shuffleArray(['b', 'c'])).toBe(['b', 'c'] || ['c', 'b'])
    })
    test('returns array of same length', () => {
        expect(array.length = arrCopy.length)
    })
})